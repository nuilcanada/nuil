NUIL app-ready prototype
Open index.html to preview. When hosted over HTTPS, manifest + service worker make it installable as a PWA. iPhone: Safari > Share > Add to Home Screen. Market statistics are prototype labels only; connect verified Canadian sources before launch.
